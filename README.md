# Next.js Fuzzy-Search Chat UI

## Setup

1. Install dependencies: `npm install`
2. Run locally: `npm run dev`
3. Deploy to Vercel (no env vars needed)

This app uses Fuse.js to match your questions against a FAQ dataset.

